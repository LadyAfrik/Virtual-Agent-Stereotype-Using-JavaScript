// jest-dom adds custom jest matchers for asserting on DOM nodes.
// These matchers allow you to make assertions like:
// expect(element).toHaveTextContent(/react/i)
// You can use these matchers to test DOM elements more easily
// For more information, refer to: https://github.com/testing-library/jest-dom

// Import jest-dom to extend Jest with these custom matchers
import '@testing-library/jest-dom';
